# 推奨: uv sync (pyproject.toml から依存解決、10-100倍高速)
# このファイルは pip install -r requirements.txt の後方互換のために維持
# 株式トレーダー拡張 - 依存パッケージ

# データ取得
yfinance>=0.2.36
requests>=2.31.0

# データ処理
pandas>=2.0.0
numpy>=1.26.0

# 機械学習
lightgbm>=4.3.0
scikit-learn>=1.4.0
scipy>=1.12.0

# テクニカル指標
pandas-ta>=0.3.14b

# Web APIサーバー
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
httpx>=0.27.0

# データキャッシュ
# sqlite3は標準ライブラリに含まれる

# テスト
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0

# 型チェック（開発用）
mypy>=1.9.0

# コードフォーマット（開発用）
ruff>=0.3.0

# 戦略最適化（Phase 2）
optuna>=4.0

# Optional: 時系列予測（Phase 3）
# darts>=0.31.0
# torch>=2.0.0

# Optional: Statistical forecasting (Phase 3)
# statsforecast>=1.7.0
# neuralforecast>=1.7.0
# nixtla>=0.6.0

# Optional: sklearn-compatible forecasting (Phase 3)
# skforecast>=0.14.0

# Optional: Zero-shot time series (Phase 3)
# chronos-forecasting>=1.4.0
# gluonts>=0.15.0
